<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- TextView控件属性 -->
    <declare-styleable name="SkinnableTextView">
        <attr name="android:background" />
        <attr name="android:textColor" />
        <attr name="android:textColorHint" />
        <!-- 字体属性 -->
    </declare-styleable>

    <!-- Button控件继承TextView，此处parent语法通过，但无效果，不像style.xml -->
    <declare-styleable name="SkinnableButton">
        <attr name="android:background" />
        <attr name="android:textColor" />
        <!-- 字体属性 -->
    </declare-styleable>

    <!-- SkinnableEditText控件 -->
    <declare-styleable name="SkinnableEditText">
        <attr name="android:background" />
        <attr name="android:textColor" />
        <!-- 字体属性 -->
    </declare-styleable>

    <!-- ImageView控件特有属性 -->
    <declare-styleable name="SkinnableImageView">
        <attr name="android:src" />
        <attr name="android:background" />
    </declare-styleable>

    <!-- SkinnableLinearLayout控件 -->
    <declare-styleable name="SkinnableLinearLayout">
        <attr name="android:background" />
    </declare-styleable>

    <!-- SkinnableLinearLayoutCompat控件 -->
    <declare-styleable name="SkinnableLinearLayoutCompat">
        <attr name="android:background" />
    </declare-styleable>

    <!-- SkinnableRelativeLayout控件 -->
    <declare-styleable name="SkinnableRelativeLayout">
        <attr name="android:background" />
    </declare-styleable>
    <!-- SkinnableNestedScrollView控件 -->
    <declare-styleable name="SkinnableNestedScrollView">
        <attr name="android:background" />
    </declare-styleable>
    <!-- SkinnableConstraintLayout -->
    <declare-styleable name="SkinnableConstraintLayout">
        <attr name="android:background" />
    </declare-styleable>
    <!-- SkinnableFrameLayout控件 -->
    <declare-styleable name="SkinnableFrameLayout">
        <attr name="android:background" />
    </declare-styleable>

    <!-- SkinnableScrollView控件 -->
    <declare-styleable name="SkinnableScrollView">
        <attr name="android:background" />
    </declare-styleable>

    <!-- SkinnableCardView控件 -->
    <declare-styleable name="SkinnableCardView">
        <attr name="android:background" />
    </declare-styleable>

    <!-- SkinnableImageButton控件 -->
    <declare-styleable name="SkinnableImageButton">
        <attr name="android:background" />
        <attr name="android:src" />
    </declare-styleable>

    <!-- SkinnableCheckBox控件 -->
    <declare-styleable name="SkinnableCheckBox">
        <attr name="android:background" />
        <attr name="android:textColor" />
    </declare-styleable>

    <!-- SkinnableRadioButton控件 -->
    <declare-styleable name="SkinnableRadioButton">
        <attr name="android:background" />
        <attr name="android:textColor" />
    </declare-styleable>

    <!-- SkinnableSwitchCompat控件 -->
    <declare-styleable name="SkinnableSwitchCompat">
        <attr name="android:background" />
        <attr name="android:textColor" />
    </declare-styleable>

    <!-- SkinnableToggleButton控件 -->
    <declare-styleable name="SkinnableToggleButton">
        <attr name="android:background" />
        <attr name="android:textColor" />
    </declare-styleable>

    <!-- SkinnableCheckedTextView控件 -->
    <declare-styleable name="SkinnableCheckedTextView">
        <attr name="android:background" />
        <attr name="android:textColor" />
    </declare-styleable>

    <!-- SkinnableAutoCompleteTextView控件 -->
    <declare-styleable name="SkinnableAutoCompleteTextView">
        <attr name="android:background" />
        <attr name="android:textColor" />
    </declare-styleable>

    <!-- SkinnableView控件（普通 <View/>） -->
    <declare-styleable name="SkinnableView">
        <attr name="android:background" />
    </declare-styleable>

    <!-- SkinnableTabLayout控件 -->
    <declare-styleable name="SkinnableTabLayout">
        <attr name="android:background" />
        <attr name="tabTextColor" />
        <attr name="tabSelectedTextColor" />
    </declare-styleable>

    <!-- SkinnableAppBarLayout控件 -->
    <declare-styleable name="SkinnableAppBarLayout">
        <attr name="android:background" />
    </declare-styleable>

    <!-- SkinnableRecyclerView控件 -->
    <declare-styleable name="SkinnableRecyclerView">
        <attr name="android:background" />
    </declare-styleable>

    <!-- SkinnableViewPager控件 -->
    <declare-styleable name="SkinnableViewPager">
        <attr name="android:background" />
    </declare-styleable>

    <!-- SkinnableHorizontalScrollView控件 -->
    <declare-styleable name="SkinnableHorizontalScrollView">
        <attr name="android:background" />
    </declare-styleable>

    <!-- SkinnableShapeableImageView控件 -->
    <declare-styleable name="SkinnableShapeableImageView">
        <attr name="android:background" />
        <attr name="android:src" />
    </declare-styleable>

    <!-- SkinnableTextSwitcher控件 -->
    <declare-styleable name="SkinnableTextSwitcher">
        <attr name="android:background" />
    </declare-styleable>

    <!-- SkinnableViewSwitcher控件 -->
    <declare-styleable name="SkinnableViewSwitcher">
        <attr name="android:background" />
    </declare-styleable>

    <!-- SkinnableViewFlipper控件 -->
    <declare-styleable name="SkinnableViewFlipper">
        <attr name="android:background" />
    </declare-styleable>

    <!-- SkinnableProgressBar控件 -->
    <declare-styleable name="SkinnableProgressBar">
        <attr name="android:background" />
    </declare-styleable>

    <!-- SkinnableSeekBar控件 -->
    <declare-styleable name="SkinnableSeekBar">
        <attr name="android:background" />
    </declare-styleable>

    <!-- SkinnableRatingBar控件 -->
    <declare-styleable name="SkinnableRatingBar">
        <attr name="android:background" />
    </declare-styleable>

    <!-- SkinnableRadioGroup控件 -->
    <declare-styleable name="SkinnableRadioGroup">
        <attr name="android:background" />
    </declare-styleable>

    <!-- SkinnableSpinner控件 -->
    <declare-styleable name="SkinnableSpinner">
        <attr name="android:background" />
    </declare-styleable>

    <!-- SkinnableListView控件 -->
    <declare-styleable name="SkinnableListView">
        <attr name="android:background" />
    </declare-styleable>

    <!-- SkinnableGridView控件 -->
    <declare-styleable name="SkinnableGridView">
        <attr name="android:background" />
    </declare-styleable>

    <!-- SkinnableExpandableListView控件 -->
    <declare-styleable name="SkinnableExpandableListView">
        <attr name="android:background" />
    </declare-styleable>

    <!-- SkinnableGridLayout控件 -->
    <declare-styleable name="SkinnableGridLayout">
        <attr name="android:background" />
    </declare-styleable>

    <!-- SkinnableTableLayout控件 -->
    <declare-styleable name="SkinnableTableLayout">
        <attr name="android:background" />
    </declare-styleable>

    <!-- SkinnableMultiAutoCompleteTextView控件 -->
    <declare-styleable name="SkinnableMultiAutoCompleteTextView">
        <attr name="android:background" />
        <attr name="android:textColor" />
    </declare-styleable>

    <!-- SkinnableTextClock控件 -->
    <declare-styleable name="SkinnableTextClock">
        <attr name="android:background" />
        <attr name="android:textColor" />
    </declare-styleable>

    <!-- SkinnableCoordinatorLayout控件 -->
    <declare-styleable name="SkinnableCoordinatorLayout">
        <attr name="android:background" />
    </declare-styleable>

    <!-- SkinnableCollapsingToolbarLayout控件 -->
    <declare-styleable name="SkinnableCollapsingToolbarLayout">
        <attr name="android:background" />
    </declare-styleable>

    <!-- SkinnableDrawerLayout控件 -->
    <declare-styleable name="SkinnableDrawerLayout">
        <attr name="android:background" />
    </declare-styleable>

    <!-- SkinnableToolbar控件 -->
    <declare-styleable name="SkinnableToolbar">
        <attr name="android:background" />
    </declare-styleable>

    <!-- SkinnableBottomNavigationView控件 -->
    <declare-styleable name="SkinnableBottomNavigationView">
        <attr name="android:background" />
    </declare-styleable>

    <!-- SkinnableFloatingActionButton控件 -->
    <declare-styleable name="SkinnableFloatingActionButton">
        <attr name="android:background" />
    </declare-styleable>

    <!-- SkinnableChip控件 -->
    <declare-styleable name="SkinnableChip">
        <attr name="android:background" />
        <attr name="android:textColor" />
    </declare-styleable>

    <!-- SkinnableChipGroup控件 -->
    <declare-styleable name="SkinnableChipGroup">
        <attr name="android:background" />
    </declare-styleable>

    <!-- SkinnableTextInputLayout控件 -->
    <declare-styleable name="SkinnableTextInputLayout">
        <attr name="android:background" />
    </declare-styleable>

    <!-- SkinnableTextInputEditText控件 -->
    <declare-styleable name="SkinnableTextInputEditText">
        <attr name="android:background" />
        <attr name="android:textColor" />
    </declare-styleable>

    <!-- SkinnableMaterialDivider控件 -->
    <declare-styleable name="SkinnableMaterialDivider">
        <attr name="android:background" />
    </declare-styleable>

</resources>